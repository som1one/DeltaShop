# Файлы, которые исполняются/читаются на Linux-сервере, всегда с LF —
# иначе checkout на Windows подсовывает CRLF и `sh` спотыкается о \r.
*.sh   text eol=lf
*.conf text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
